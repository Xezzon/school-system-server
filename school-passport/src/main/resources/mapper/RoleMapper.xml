<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="indi.xezzon.school.passport.repository.RoleMapper">
    <resultMap id="BaseResultMap" type="indi.xezzon.school.passport.model.Role">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="description" jdbcType="VARCHAR" property="description"/>
    </resultMap>
    <sql id="Base_Column_List">
        id, name, description
    </sql>
    <select id="selectByAccountId" resultType="indi.xezzon.school.passport.model.Role">
        SELECT
        <include refid="Base_Column_List"/>
        FROM role RIGHT JOIN
        (SELECT role_id
        FROM account_role
        where account_id = #{accountId}) ar
        ON ar.role_id = role.id;
    </select>
    <select id="list" resultType="indi.xezzon.school.passport.model.Role">
        SELECT
        <include refid="Base_Column_List"/>
        FROM role;
    </select>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
        delete
        from role
        where id = #{id,jdbcType=INTEGER}
    </delete>
    <delete id="deleteAccountRoleRel">
        DELETE
        FROM account_role
        WHERE account_id = #{accountId}
          AND role_id = #{roleId}
    </delete>
    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="indi.xezzon.school.passport.model.Role"
            useGeneratedKeys="true">
        insert into role (name, description)
        values (#{name,jdbcType=VARCHAR}, #{description,jdbcType=VARCHAR})
    </insert>
    <insert id="insertAccountRoleRel">
        INSERT INTO account_role VALUES
        <foreach collection="rolesId" item="roleId" open="(" close=")">
            (#{accountId}, #{roleId})
        </foreach>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="indi.xezzon.school.passport.model.Role">
        update role
        <set>
            <if test="name != null">
                name = #{name,jdbcType=VARCHAR},
            </if>
            <if test="description != null">
                description = #{description,jdbcType=VARCHAR},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>
    <update id="updateAccountRoleRel">

    </update>
</mapper>