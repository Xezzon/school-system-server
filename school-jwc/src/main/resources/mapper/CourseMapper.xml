<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="indi.xezzon.school.jwc.repository.CourseMapper">
    <select id="list" resultType="indi.xezzon.school.common.model.Course">
        SELECT c.id, c.name, c.credit, t.id as `teacher.id`, t.name as `teacher.name`
        FROM course c
        INNER JOIN teacher t
        WHERE c.teacher =t.id
        LIMIT #{offset}, #{limit};
    </select>
    <select id="count" resultType="java.lang.Integer">
        SELECT COUNT(id) FROM course;
    </select>
</mapper>